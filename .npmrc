# Keep workspace-internal packages linked from source (no publish step needed).
link-workspace-packages=true
# Surface peer dependency issues instead of silently auto-installing them.
auto-install-peers=true
